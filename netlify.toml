[build]
  publish = "dist"
  command = "npm run build"

# COOP/COEPは付けない（numThreads=1でSharedArrayBuffer不使用。
# クロスオリジン分離が有効だと旧iOS WebKitの共有メモリ成長バグで推論に失敗する）

# SPAフォールバック
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
